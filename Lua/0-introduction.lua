-- Lua es un lenguaje de programacion interpretado, extramadamente rapido, liviano y muy sencillo.
-- Tanto asi, que solo existen 21 palabras reservadas:

-- and       break     do        else         elseif
-- end       false     for       function     if
-- in        local     nil       not          or
-- repeat    return    then      true         until     while

-- (Si, esos son todos)

-- Lua aunque puede utilizarse de manera individual para realizar tareas simples,
-- fue diseñado para ser integrado en otras aplicaciones. Por ejemplo: el scripting
-- de Roblox, Garry's Mod y otros juegos; compiladores de LaTeX; motor de base de datos
-- como Redis; o aplicaciones de servidor.

-- Tanto es asi, que hasta posee una API para aplicaciones escritas en C/C++, en pocas
-- palabras significa que puedes ejeuctar Lua de manera nativa en cualquier aplicacion
-- escrita en C/C++ usando sus respectivas librerias.

-- De la misma forma, tambien posee una libreria en Python.

-- Lua ademas de todo eso, es extremadamente rápido, gracias a que realiza compilacion en
-- bytecode que luego la maquina virtual de Lua ejecuta (a lo Java).

-- Para empezar, simplemente instala Lua en Windows o Linux:

-- $ sudo apt install lua5.3

print("Felicidades, has instalado Lua")

-- Por cierto, esta es la forma de crear comentarios de liena, con '--'

--[[
    Y para comentarios de bloque,
    se añaden corchetes dobles.
    Eso.
]]
