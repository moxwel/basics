/* Una lista consiste en una estructura de datos
 * dinamica, es decir, se pueden insertar o
 * eliminar elementos, ademas de estar "ordenadas"
 * segun la posicion en la que estan ubicadas en este.
 * Generalmente, los elementos de una lista son de un
 * unico tipo de dato.
 *
 * Se deberia poder:
 * - Añadir o eliminar elementos.
 * - Ser capaz de acceder a cualquier elemento
 * - Dado una posicion, se debe poder acceder al
 *   elemento posterior o anterior
 * - Debe poseer un cursor. Es necesario para poder
 *   tener en cuenta la idea de "posicion actual".
 *
 * Se pueden hacer de dos tipos:
 * - Basada en arreglos
 * - Enlazadas
 */

int main() {
    return 0;
}
